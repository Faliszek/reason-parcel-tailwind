{
  "name": "reason-parcel-tailwind",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "reason-react": "^0.7.0"
  },
  "dependencies": {
    "tailwindcss": "^1.2.0",
    "bs-platform": "^7.1.1",
    "parcel-bundler": "^1.12.3"
  },
  "scripts": {
    "buckle:up": "bsb -make-world -w",
    "start": "parcel ./public/index.html --port=3000",
    "fresh:start": "rm -rf .cache && rm -rf dist && parcel ./public/index.html --port=3000"
  }
}
